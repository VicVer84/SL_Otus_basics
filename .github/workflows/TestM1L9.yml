name: TestM1L9

on:
  push:
    branches: [ M1L9 ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          ref: M1L9
                    
      
      - name: Build
        run: cd /home/runner/work/SL_Otus_basics/SL_Otus_basics/M1L9 && mkdir Build && cd Build && cmake .. && cmake --build .
        
      - name: Run tests
        run: /home/runner/work/SL_Otus_basics/SL_Otus_basics/M1L9/bin/statistics.test
